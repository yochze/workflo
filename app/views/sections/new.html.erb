   <%= form_for [@project, @section], :remote => true do |builder| %>
          <%= builder.text_field :title %>
          <%= builder.text_field :info %>
          <%= builder.submit %>
   <% end %>  

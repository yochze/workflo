$(document).ready(function(){
  var new_module = "<%= escape_javascript(render :partial => 'new_module_icon', :locals => { :section => @section }) %>"
  $('ul.module-circle li:last').before(new_module);

  $('a#new-member-modal').colorbox({
  html:"<b>asdf</b>",
  transition:"none",
  speed:800,
  fadeOut:200,
  width:450,
  height:300,
  fixed:true, 
  className:'issue-modal'
 });

});

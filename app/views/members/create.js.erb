$(document).ready(function(){
  var new_member = "<%= escape_javascript(render :partial => 'new_member_icon', :locals => { :project_member => @member, :project => @project }) %>"
  $('ul#team-circle li:last').before(new_member);
	$('a#new-member-modal').colorbox({
	  html:"<b>asdf</b>",
	  transition:"none",
	  speed:800,
	  fadeOut:200,
	  width:450,
	  height:300,
	  fixed:true, 
	  className:'issue-modal'
	 });
});